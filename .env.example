# No environment variables are needed yet.
# When booking/payments are added, put placeholder NAMES here (never real values),
# and set the real values in Vercel → Project → Settings → Environment Variables.
# NEXT_PUBLIC_CAL_LINK=
